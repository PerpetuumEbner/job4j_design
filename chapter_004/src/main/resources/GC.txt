2. Создать несколько объектов User и руками рассчитать сколько он будет занимать памяти.
Заголовок - 16 байт
Поле name - 4 * 3 байт
Поле age - 4 байта
Всего - 16 + 12 + 4 = 34 байт

3. Нужно найти информацию. Сколько памяти занимает пустой объект без полей.
В 32х разрядной системе размер заголовка - 8 байт, 64-х разрядной системе - 16 байт.

4. Установил значение -Xms5m -Xmx5m

5. Объяснить поведение программы в текстовом файле.
- Выводится на экран информация о свободной памяти, общей памяти и размер объекта User;
- В цикле создаются объекты User с полями name и age;
- При заполнении выделенной памяти происходить удаление объекта;
- Повторно выводится информация о памяти, размер объекта User изменился.